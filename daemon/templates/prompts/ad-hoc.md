A user is requesting help via Discord. Use ALL available MCP tools to fulfill this request.
Do NOT just describe what you could do — actually USE the tools to get real data and perform actions.

<user_request user_id="{userId}">
{text}
</user_request>

{formatRules}
